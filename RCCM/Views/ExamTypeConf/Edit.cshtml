@model UpdateExamTypeConfViewModel
@{
    ViewData["Title"] = "Edit Exam";
    Layout = "_AdminLayout";
}

<div class="container my-4">
    <h2 class="text-center mb-4">تحديث معلومات الاختبار</h2>

    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow custom-card border-primary">
                <div class="card-header bg-custom-primary text-white text-center">
                    <h3>تحديث بيانات الاختبار</h3>
                </div>
                <div class="card-body custom-card-body">
                    <form asp-action="EditExamTypeConf" method="post">
                        <div class="mb-3">
                            <label asp-for="Title" class="form-label">عنوان الاختبار</label>
                            <input asp-for="Title" class="form-control" />
                        </div>

                        <div class="mb-3">
                            <label asp-for="StartDate" class="form-label">بداية فترة الاختبار</label>
                            <input asp-for="StartDate" type="date" class="form-control" />
                        </div>

                        <div class="mb-3">
                            <label asp-for="EndDate" class="form-label">نهاية فترة الاختبار</label>
                            <input asp-for="EndDate" type="date" class="form-control" />
                        </div>

                        <input type="hidden" asp-for="ExamTypeConfId" />

                        <div class="d-flex justify-content-between mt-4">
                            <button type="submit" class="btn btn-success">تحديث بيانات الاختبار</button>
                            <a class="btn btn-primary" asp-action="Index">العودة لقائمة الاختبارات</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
